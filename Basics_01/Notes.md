# React Basics Notes

There are functions that create elements in our HTML through JavaScript in the React library .

- React.createElement(_,{},_) ;
- ReactDOM.createElement(doucment.getElementById("root")) .
- root.render(React component )

Some Basic Information for React Notes :

1. What is react CDN ? <br>
   It is a Alternate way to use react the normal production way is to use npx create .
   We can use React CDN Links but direcly importing react before the local javascript file, this helps us directly apply react to specific components.

2. Why is React called a Library ? <br>
   React can work independently in a small portion of a Application that's why it is called a Library , not a framework .
